timezone: UTC
encoding: utf-8
redcarpet:
  extensions: [smart]
kramdown:
  input: GFM
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
